#ifndef __ASM_ARC_SWAB_H
#define __ASM_ARC_SWAB_H

/* Our architecture doesn't support assembly instructions to do the byte swap
 * FIX ME: Use an optimized solution rather than using the generic functions
 */

#endif
